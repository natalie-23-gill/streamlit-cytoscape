import React from "react"
import ReactDOM from "react-dom"
import StCytoscapeComponent from "./StCytoscapeComponent"

ReactDOM.render(
  <React.StrictMode>
    <StCytoscapeComponent />
  </React.StrictMode>,
  document.getElementById("root")
)
